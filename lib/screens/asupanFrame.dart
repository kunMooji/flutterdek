import 'package:flutter/material.dart';

class AsupanScreen extends StatelessWidget {
  const AsupanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Asupan Screen'),
    );
  }
}